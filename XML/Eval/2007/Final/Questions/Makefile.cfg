#-*-makefile-*-

TEX := xml-final

BASE_DIR := ../../../2008/Exercises/Merging
XML_DIR  := ${BASE_DIR}
XSLT_DIR := ${BASE_DIR}

override DEF += \def\java{${JAVA} }\def\saxlib{${SAXON} }\def\xmllint{${XMLLINT} }\def\xmlURL{${XML_URL}}\def\xsltURL{${XSLT_URL}}\def\XMLdir{${XML_DIR}}\def\XSLTdir{${XSLT_DIR}}
